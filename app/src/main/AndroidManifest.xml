<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.canyinghao.canaccess">



    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application android:allowBackup="true" android:label="@string/app_name"
        android:name=".App"
        android:icon="@mipmap/ic_launcher" android:theme="@style/Theme.DesignDemo">



        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.DetailActivity" />
        <activity android:name=".activity.set.AppListActivity" />
        <activity android:name=".activity.set.IgnoreTextActivity" />
        <activity android:name=".activity.set.SetActivity" android:label="setting"
            android:theme="@style/MaterialSettingsTheme" />
        <activity android:name=".activity.set.SetNotifyActivity" android:label="Notifysetting"
            android:theme="@style/MaterialSettingsTheme" />


        <service android:label="@string/acc_service_name" android:name=".service.CanAccessibilityService" android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice" android:resource="@xml/phone_accessibility" />
        </service>


        <receiver android:name=".receiver.PhoneChangedReceiver" >
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.STATE_CHANGE" />

            </intent-filter>
        </receiver>
    </application>

</manifest>
